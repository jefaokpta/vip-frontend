import {Component} from '@angular/core';

@Component({
    selector: 'app-dialplan-page',
    standalone: true,
    template: `
        <div class="card">
            <div class="font-semibold text-xl mb-4">Dialplan Page</div>
            <p>Manage and configure a single dialplan for your application.</p>
        </div>`
})
export class DialplanPage {
}
