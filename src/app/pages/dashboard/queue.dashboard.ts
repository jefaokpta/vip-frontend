import { Component } from '@angular/core';

@Component({
    selector: 'app-queue-dashboard',
    standalone: true,
    template: ` <div class="card">
        <div class="font-semibold text-xl mb-4">Painel de Filas</div>
        <p>Este é a página de Painel de Filas.</p>
    </div>`
})
export class QueueDashboard {}
